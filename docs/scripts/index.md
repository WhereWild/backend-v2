# Scripts

This section documents the one-off data processing scripts in `scripts/`.
Each page is generated from the script’s module docstring.

Common entry points include building the taxonomy tree, enriching with GIS
data, generating summary stats, and building index parquets.
